@darkenVal : 10%;
@desaturateVal : 10%;
@fadeVal: 90%;
/* Dull green as it's excessively bright by default*/
@green : fade(desaturate(darken(#21BA45, @darkenVal, relative), @desaturateVal, relative), @fadeVal);
